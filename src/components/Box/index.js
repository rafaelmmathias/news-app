import styled from 'styled-components';
import {
  color,
  space,
  border,
  position,
  flexbox,
  layout,
  grid
} from 'styled-system';

const Box = styled.div`
  ${color}
  ${space}
  ${border}
  ${position}
  ${flexbox}
  ${layout}
  ${grid}
`;

export default Box;
